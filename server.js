const express = require("express");
const { ApolloServer } = require("apollo-server-express");

// 1. import graphql schema
// 2. create new instance of apollo server with graphql schema
// 3. call function to start server
